export const config = {
  // Роздільність, у якій рендеримо PDF для кропів фрагментів (висока — задля якості).
  cropRenderScale: 3.0,
  // Роздільність зображення, що шлемо в Gemini (нижча — щоб не зростала вартість/час).
  // bbox нормалізований (0..1000), тож кропи беремо з hi-res полотна незалежно від цього.
  pdfRenderScale: 1.5,
};
